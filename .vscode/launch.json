{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run poligrapher-app",
            "type": "debugpy",
            "request": "launch",
            "module": "poligrapher_app",
            "python": "${workspaceFolder}/.venv/bin/python",
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal"
        }
    ]
}
