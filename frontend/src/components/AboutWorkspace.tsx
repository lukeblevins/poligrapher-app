import { SOURCES } from "./AttributionModal";
import { useThemeSeedColor } from "../hooks/useTheme";

export function AboutWorkspace() {
  const { seedColor, hasCustomSeedColor, setSeedColor, resetSeedColor } = useThemeSeedColor();

  return <main className="min-w-0 flex-1 overflow-y-auto bg-[var(--md-sys-color-surface)] px-5 py-8 sm:px-8"><section className="mx-auto max-w-3xl"><p className="section-kicker">Settings</p><h1 className="mt-1 font-display text-3xl font-semibold">About Privacy Policy Analyzer</h1><p className="mt-4 max-w-2xl text-sm leading-6 text-[var(--md-sys-color-on-surface-variant)]">A research workspace for configuring privacy-policy sources, running analyses, and reviewing privacy assessments.</p><section className="mt-8" aria-labelledby="theme-heading"><h2 id="theme-heading" className="font-display text-xl font-semibold">Theme color</h2><p className="mt-1 text-sm leading-6 text-[var(--md-sys-color-on-surface-variant)]">Choose a seed color for the app’s primary Material color family.</p><div className="m3-theme-picker mt-4"><input aria-label="Theme seed color" type="color" value={seedColor} onChange={(event) => setSeedColor(event.currentTarget.value)} /><div className="min-w-0 flex-1"><p className="text-sm font-semibold text-[var(--md-sys-color-on-surface)]">Custom theme color</p><p className="text-sm text-[var(--md-sys-color-on-surface-variant)]">{hasCustomSeedColor ? seedColor.toUpperCase() : "Default teal"}</p></div>{hasCustomSeedColor && <button type="button" className="m3-theme-reset" onClick={resetSeedColor}>Reset</button>}</div></section><h2 className="mt-10 font-display text-xl font-semibold">Data sources and attribution</h2><div className="mt-4 grid gap-3">{SOURCES.map((source) => <article key={source.name} className="rounded-[var(--md-sys-shape-corner-medium)] bg-[var(--md-sys-color-surface-container-low)] p-4"><a href={source.href} target="_blank" rel="noreferrer" className="text-sm font-semibold text-[var(--md-sys-color-primary)] hover:underline">{source.name}</a><p className="mt-1 text-sm leading-5 text-[var(--md-sys-color-on-surface-variant)]">{source.description}</p></article>)}</div></section></main>;
}
